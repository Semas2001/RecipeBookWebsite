import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const CBR = localFont({
  src: "./fonts/CBR.woff",
  variable: "--font-CBR",
  weight: "100 900",
});
const CBB = localFont({
  src: "./fonts/CBB.woff",
  variable: "--font-CBB",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${CBR.variable} ${CBB.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
